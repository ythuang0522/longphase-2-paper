Precise local edit of attached 16:9 scientific figure. Keep panels a and b, layout, typography, small letters, architecture and all other content unchanged. Only simplify panel c's feature representation and reduce panel d's read density.

PANEL C: Remove EVERY multicolored segmented vector strip/array throughout panel c: the horizontal strip beneath Node features; horizontal strip beneath Edge features; small multicolor vertical blocks on either side of the Local attention graph; and multicolor blocks on either side of Window-wide attention graph. No rainbow bars or heatmap cells remain.
Replace input feature display with DIRECT SCIENTIFIC ANNOTATIONS, not new abstract arrays:
Node features heading above input graph. Three short names "Phase evidence", "Variant type", "Genomic context", then "...", neatly arranged above graph in reserved whitespace. ONE fine neutral leader from this group points directly to the center upper-row C node, with subtle amber circular halo around ONLY that node. Keep circles on two perfectly horizontal aligned rows, no displacement.
Edge features heading below input graph. Three short names "Link strength", "Genomic distance", "Phase set relation", then "...", positioned below graph. ONE fine neutral leader points precisely to the midpoint of the lower-row G-to-C edge, with subtle amber highlight along that edge only. Not to a node. Do not use colored vector strips.
In attention branches let node-to-node arrows/local neighborhood and window-wide graph show the operations. Simply remove peripheral colored arrays; preserve graph nodes and arrows and GATv2/Transformer labels. Preserve additive fusion, compact FFN, residual connections and clear unphased-SNP output unchanged.

PANEL D: Keep both Haplotype1 and Haplotype2 illustrated rails unchanged. Under "Haplotagging reads", replace the crowded five read tracks with ONLY THREE horizontal read tracks: two cyan-tagged reads and one coral-tagged read. Space all three at identical generous intervals from below title to bottom margin, with no overlap among variant circles, rectangular variants and methylation lollipops. Each read symbol stays centered on its respective read; methylation stem intersects that read only, never neighbor. Use approximately y846,y879,y912 on current941px canvas. Three read tracks total, not four or five. Preserve gray read lines, cyan/coral variant tags, dark methylation glyphs, small flank ellipses. Keep label.
No other changes, no new captions, no added arrays. Preserve 16:9 and full scientific detail elsewhere.

Final correction: Local correction to panel c ONLY. Keep all other image content pixel-identical including three spacious haplotagged reads. Move the 'Edge features' leader arrow endpoint AWAY from lower C circle: point it to the MIDPOINT of the horizontal edge connecting leftmost lower G circle and next lower C circle. Highlight just that edge segment in pale amber, do not highlight a node for edge features. Remove broad yellow highlight around entire C/T vertical column; keep only a small circular amber halo around upper C for Node features. Restore a fine dark-gray residual bypass from the input graph's outgoing branch point, route underneath both local and window-wide attention graphs, then turn upward to enter the FIRST additive-fusion plus from below. This is the input residual connection; preserve the existing FFN residual loop and second sum. No changes to feature names, graphs or other panels.
