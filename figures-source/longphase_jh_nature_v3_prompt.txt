Create a substantially redesigned scientific methods figure for a genomics research paper. Use the provided previous draft as subject reference only. Target the restrained precision, hierarchy and typography of a Nature Methods research figure. Render at the highest practical resolution, preferably 3200 x 2400 or greater, landscape 4:3. Pure white, flat vector-like geometry, no texture, shadows, gradients, bevels or rounded dashboard cards. Generous margins. Arial/Helvetica-like type, sentence-case headings, small bold lowercase panel letters. All text must be crisp and readable. No journal logo, no big title, no claim of benchmark improvement.

SCIENTIFIC STORY: LongPhase integrates SNVs, small indels, structural variants and allele-specific methylation in a weighted phasing graph; it performs multi-neighbor haplotype voting and read-based correction. Phasing diagnostics enable an OPTIONAL downstream GNN that removes unreliable phase assignments. GNN is one module and must occupy at most 20 percent of the composition. Compare is evaluation, not inference.

LAYOUT: three horizontal tiers, panel a at top spanning width (~22% height); panel b dominant middle left (~58% width and ~52% height), panel c middle right (~36% width and ~52% height); panel d narrow bottom spanning width (~18% height). No enclosing outer frames. Thin pale dividing rules only.

a heading "Multimodal phasing workflow".
A compact left-to-right workflow:
"Long reads + reference" and "SNV/indel + SV calls" converge into "LongPhase phase".
A small lower input branch "MM/ML tags" → "modcall" → "5mC calls" feeds the SAME phase module.
"LongPhase phase" → "Phased VCFs".
From "Phased VCFs", a solid direct line reaches "haplotag" and "Tagged reads". A thin dashed optional branch goes via "GNN correction" and rejoins before haplotag. Label branch "optional".
Keep graph/DOT output as a small text label under phase "Weighted graph + PE, H1, H2". A dashed connector routes that evidence to the GNN branch. Phased VCFs exist before haplotag.

b heading "Weighted graph phasing".
This is the central illustration. Maintain exactly six aligned locus columns through reference, reads, graph and output. Column labels in this exact order "SNV", "Indel", "SV", "5mC", "SNV", "SNV". The upper allele states across columns are A, +T, INS, filled circle, C, G. The lower allele states are G, dash, REF, open circle, T, A. Colored upper and lower states must remain consistent at EVERY stage. Use teal-blue #238CA6 for haplotype 1 and vermilion #D66A54 for haplotype 2. Locus type indicated by shape as well: small round SNV nodes containing the nucleotide, indel small rectangle containing +T or dash, SV wider rectangle INS or REF, methylation filled vs open circles. No same allele in both rows.
Top: reference coordinate ruler then four thin light gray horizontal read segments, each spanning some of the six columns. Reads carry exact same-position allele observations, two consistent with each haplotype. One faint inconsistent observation may be shown in gray. No free-floating letters, no misplaced columns.
Middle: two rows of the six allele nodes connected by solid matching-haplotype edges with several arcs to non-adjacent loci. A few weak gray diagonal alternative links between DIFFERENT locus columns; NEVER edges between two alleles at the SAME locus. Edge thickness encodes support. No numeric edge values needed.
A slim label beside graph "Multi-neighbor voting". Under graph show an unobtrusive two-step annotation "Initial phase assignment → Read-based correction".
Bottom: exactly the same two rows of six output alleles, labels "Haplotype 1" and "Haplotype 2"; consistent colors and all same alleles retained.
Two small marginal callouts with fine leader lines to graph: "Base-quality weighting" and "Repeat / alignment filtering". Do not invent equations or counts.

c heading "Optional uncertainty correction".
Small compact inset, abundant white space. Start with two accurate vote bars labeled "H1 = 3" and "H2 = 1", lengths in 3:1 ratio, both in neutral gray to avoid identifying them with entire haplotype tracks. Label "PE = 0.811 bits". Under bars, small statement "PE ≥ 0.80 triggers a local window". The correct entropy for p=0.75,0.25 is 0.811, not 0.2.
Arrow to a miniature local graph: paired allele nodes at five positions, sparse edges, central pair outlined amber. Label "±20 variants". Next a small compact rectangle "GNN" with subtitle "Graph + sequence context".
Arrow to label "P(error) ≥ 0.30". Then exact "0|1 → 0/1" and subtitle "Remove phase assignment". Do not show allele flipping or new variant calls.
At bottom depict a single bracket "One phase set" over a short graph with central amber uncertain bridge; below show central bridge removed and two disconnected groups bracketed "PS 1" and "PS 2". Label "Split disconnected phase sets". Optional correction removes phasing but retains genotype. All these marks must fit cleanly; this is subordinate to b.

d heading "Outputs and evaluation".
Left half: compact two-haplotype miniature with adjacent text "Phased SNVs, indels, SVs and 5mC"; below, two gray reads tagged "HP:1  PS:100" and "HP:2  PS:100". Both share PS:100 because HP differs within the same block. Label "Tagged BAM/CRAM".
Right half: small evaluation workflow "Phased VCF" and "Truth VCF" both point INTO "compare"; then arrow to "Switch error • Hamming error • N50". This branch is labeled "Evaluation". Do not feed evaluation from modcall, do not feed truth into phasing.
Single understated legend bottom edge: teal "Haplotype 1", vermilion "Haplotype 2", gray thin line "Read support", amber outline "Uncertain locus", filled/open black circles "Modified / unmodified".

Professional detail requirements: perfectly aligned columns, consistent node dimensions and line weights, arrowheads ending before nodes, no collisions or lines through labels, no repetitive file-icon cards, no oversize bold headings, balanced whitespace, narrow palette, color-blind redundancy. No unsupported performance claims. Retain scientific content but fully redesign the previous draft.

Final refinement requirements applied with the built-in image-generation editor:
Keep the phased VCF and weighted graph as inputs to the optional GNN module. Remove the bridge across the unphased locus in the split-phase-set example. Rebuild the lower graph section using exactly six explicitly numbered loci and exactly six allele nodes per haplotype in both the graph and the corrected output: A/+T/INS/filled circle/C/G and G/deletion/REF/open circle/T/A. Do not insert a seventh allele column or letter O as a methylation state. Preserve the other panels and restrained journal-style design.

Final output: longphase_jh_nature_v3.png (1448 x 1086 raster illustration).
