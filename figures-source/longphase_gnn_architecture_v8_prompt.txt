Edit the supplied scientific figure. Change ONLY panel d, "GNN refinement". Preserve panels a, b, c, e and their illustrations, wording, colors and exact five-stage left-to-right flow. If necessary modestly widen panel d and overall canvas, never shrink other panels or omit their content. Keep pure opaque white, crisp precise vector-like molecular glyphs, teal and coral haplotypes, charcoal text, subtle ochre for uncertainty. Highest practical resolution. No enclosing boxes, cards, shadows, numerical thresholds, feature counts, layer counts, probabilities, or software flowchart. Panel d must remain ONE mandatory module of whole method, not become the entire figure.

Replace old panel d generic graph and raw sequence strips with an elegant vertically organized scientific illustration of a GPS-style hybrid graph model:

TOP: small direct heading "Node and edge features". Draw an allele graph with two rows of five nodes; top A T C G T, bottom G C T A C; strong within-track edges and a few thin competing interlocus links, no vertical links between same-locus alternatives. Central locus softly highlighted. Attach a tiny four-segment feature strip to a representative node with a fine leader. Beside it list the conceptual node categories in small clear type on four short lines: "Phase evidence", "Variant type", "Genomic context", "Graph structure". Attach a small three-segment strip to an edge with a leader, nearby concise labels "Link strength", "Genomic distance", "Phase-set relation". These are direct graphic annotations, NOT text boxes. No invented feature count. Genomic context is engineered context features, not a separate learned raw-sequence encoder. Do not show naked nucleotide sequence strips as model inputs. Node features include derived sequence properties, phase votes, variant identity and graph position. Edge features describe evidence and relationships.

MIDDLE: two small graph views arranged SIDE BY SIDE, both receiving thin arrows from the annotated graph above, clearly PARALLEL rather than sequential. Left view label "Local graph attention" with smaller "GATv2". Show a center node receiving short directed arrows ONLY from graph-linked neighbors, and tiny edge feature marks influencing those arrows. Right view label "Window-wide attention" with smaller "Transformer". Show nodes across a small window, with a few long spanning arcs reaching beyond immediate graph neighbors. These are alternative views of the SAME window, not separate chromosomes. Subtle grey node scaffolds, colored selected nodes, no neural-network/brain icon. Both branches must converge with TWO fine arrows at a small shared node-feature strip; direct label "Joint representation". Never draw local then global in serial. Avoid giant architecture blocks.

BOTTOM: one narrow down arrow, small direct label "Detect unreliable phase", then two short haplotype tracks using same five alleles: top A T C G T, bottom G C T A C. Only central C and T are neutral gray outlined circles and completely disconnected from tracks. Left segment contains two connected nodes; right segment contains two connected nodes. ABSOLUTELY NO edges, dashed bridges or arrowheads touching the central gray alleles. All alleles retained; only phase removed. Caption "Retain alleles; withhold uncertain phase". Keep other four positions colored consistently. Output toward panel e remains required main flow.

Use visual hierarchy to make the parallel local/window-wide architecture the main new detail in d. Maintain readable uncluttered direct annotations despite richer scientific content. Preserve EXACTLY the other four panels including corrected parallel/crossed pairing diagrams in c. This is a targeted journal-figure revision based on verified architecture, not a redesign of the method.
